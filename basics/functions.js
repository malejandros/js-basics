// Performing a task
function saludo(name, lastName) {
  console.log(`Hello ${name} ${lastName}`);
}

// Calculating a value
function square(number) {
  return number * number;
}

saludo('Manuel', 'Salinas');
saludo('Raul');

console.log(square(2));