// Variables por defecto se inicializan como "undefined"
// Reglas para declarar variables
// No pueden contener palabras reservadas (if, class, var, const, etc. )
// El nombre de la variable debe estar relacionada con el contenido por ejemplo: nombre, edad y no n o e
// No pueden iniciar con un numer: 1name
// No pueden contener espacios 
// Por convención usar camelNotation: firstName
// El nombre de la variable es case-sensitive


let name = 'Manuel';
let lastName = 'Salinas';

console.log(name);
console.log(lastName);